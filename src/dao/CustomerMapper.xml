<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="dao.CustomerMapper" >
    <sql id="cust_where">
        <where>
            <if test="custName != null and custName != ''">
                and c.cust_name like '%${custName}%'
            </if>
            <if test="custSource != null and custSource != ''">
                and c.cust_source = #{custSource}
            </if>
            <if test="custIndustry != null and custIndustry != ''">
                and c.cust_industry = #{custIndustry}
            </if>
            <if test="custLevel != null and custLevel != ''">
                and c.cust_level = #{custLevel}
            </if>
        </where>
    </sql>
    <select id="findCustomerByDict" parameterType="pojo.QueryVo" resultType="pojo.Customer">
        SELECT c.cust_id,c.cust_name,c.cust_linkman,c.cust_phone,c.cust_mobile,c.cust_address,c.cust_zipcode,
        b1.dict_item_name cust_source,b2.dict_item_name cust_industry,b3.dict_item_name cust_level
        from customer c
        left join base_dict b1 on c.cust_source = b1.dict_id
        left join base_dict b2 on c.cust_industry = b2.dict_id
        left join base_dict b3 on c.cust_level = b3.dict_id
        <include refid="cust_where"/>
        limit #{start},#{size};
    </select>

    <select id="countCustomerByDict" resultType="int" parameterType="pojo.QueryVo">
        SELECT count(*)
        from customer c
        left join base_dict b1 on c.cust_source = b1.dict_id
        left join base_dict b2 on c.cust_industry = b2.dict_id
        left join base_dict b3 on c.cust_level = b3.dict_id
        <include refid="cust_where"/>
    </select>

    <select id="findCustomerById" parameterType="long" resultType="pojo.Customer">
        SELECT * from customer where cust_id = #{id}
    </select>

    <select id="updateCustomerById" parameterType="pojo.Customer" >
        update customer
        <set>
            <if test="cust_name != null and cust_name != ''">
                cust_name = #{cust_name},
            </if>
            <if test="cust_source != null and cust_source != ''">
                cust_source = #{cust_source},
            </if>
            <if test=" cust_industry != null and  cust_industry != ''">
                cust_industry = #{cust_industry},
            </if>
            <if test="cust_level != null and cust_level != ''">
                cust_level = #{cust_level},
            </if>
            <if test="cust_linkman != null and cust_linkman != ''">
                cust_linkman = #{cust_linkman},
            </if>
            <if test="cust_phone != null and cust_phone != ''">
                cust_phone = #{cust_phone},
            </if>
            <if test="cust_mobile != null and cust_mobile != ''">
                cust_mobile = #{cust_mobile},
            </if>
            <if test="cust_zipcode != null and cust_zipcode != ''">
                cust_zipcode = #{cust_zipcode},
            </if>
            <if test="cust_address != null and  cust_address!= ''">
                cust_address = #{cust_address},
            </if>
        </set>
        where cust_id = #{cust_id}
    </select>

    <delete id="deleteCustomerById" parameterType="long">
        DELETE FROM customer where cust_id = #{id};
    </delete>
</mapper>